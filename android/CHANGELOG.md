# 墨织 Android 修改日志

更新日期：2026-09-26。本文记录已落地功能与实际验证，后续计划见 [待实现内容](TODO.md)。早期各阶段文档保留当时范围，当前状态以本文件及对应构建回执为准。

## 自定义封面 · 2026-09-26

版本 `0.0.13-custom-covers`（13），数据库 Schema 10。

- 修复资料库搜索后立即打开笔记菜单时键盘收起的时序；模板测试按稳定键滚动，避免把未进入可见区的模板判为缺失。
- 新建笔记和资料库“更换封面”共用封面编辑器：本地图片、取景缩放与位置、六位自定义色号、独立封面标题、副标题、文字开关及书签标签／底部色带／简约标题三种排版。
- 内置纯色封面改为更清晰的书签标签构图，封面文字随缩略尺寸缩放；笔记名称仍在封面外支持系统字号。
- JPG／PNG／WebP／HEIF 图片经系统文件选择器导入，读取上限 20 MB；应用保存最长边 1024px 的 JPEG 副本，单图不超过 180 KB，去除原始图片元数据。不依赖原图 URI 长期授权。
- 图片取景可反复修改或重置，移除图片后可保留配色与文字；切回内置样式保留最近一份自定义设计，方便重新使用。取消不写库。
- 自定义封面随整本复制独立保存，随完整备份恢复；Schema 10 新增独立封面表，读取 Schema 6—9 旧备份。封面不占正文页，不改变笔迹、摘要及稳定身份。
- 内部封面图片预算合计 32 MB；普通 `.iwpage`／`.iwnote` 内容副本仍不携带封面，完整迁移使用 `.iwbackup`。

本地核心 199 项、数据库 129 项、界面 57 项通过；lint 为 0 错误、53 警告。实际最终构件、升级及 CI 检查见 `dist/Custom-Covers/交付回执.md`。本轮实机新增 T25—T27，升级和备份重点复测 T01、T14。

## UI-R2 · 统一工作台与知识连接 · 2026-09-26

版本：`0.0.11-ui-r2`，versionCode `11`，数据库 Schema `9`。本轮最终构件与检查结果以交付目录回执为准；旧版 A3.5 的统计不用于证明本轮通过。

- 修复 400dp 窄屏大字号下资料库工具栏挤压；次要操作收进更多菜单，保留标题与新建入口。
- 按 UI-R2 统一白色／灰色／墨绿主题、字体层级及按需打包的 Material Symbols；保留原生 Compose 和 Ink。
- 可视化新建配置页，封面与内页分开；增加康奈尔窄栏／宽栏／空白、错题整理、推导、每日学习模板，预览与原生纸面共用作者坐标定义；记住最近的创建配置。
- 新建使用稳定请求和事务回执，结果不确定时核对原请求；取消配置页不写入半本笔记。
- 文档入口与笔盒合并为双层工具区，页码／缩放移到页脚；当前笔、橡皮、框选、撤销等保持直接可达。系统返回与区域链接跳转等待活动笔划完成。
- 笔盒按笔记记住，自定义十六进制颜色，并提供显式“设为默认笔盒”；橡皮轮廓采用双对比边缘。仅修改荧光标注时延续有效人工索引，不复活已失效索引。
- 新增可独立于卡片的手写区域锚点及区域链接；链接按稳定身份保存，反向引用由同一条正式引用反查。支持实时卡片引用、固定修订摘录和显式确认候选提及。
- 新增卡片手工属性、别名、按标签／状态筛选的集合；属性可在本次会话撤销。集合是视图，不复制摘要正文。
- 新增一跳／二跳局部关联图与关系列表，图形位置不改变关系；独立脑图共享卡片正文并分别保存层级和位置，自由白板的摆放与装饰线不成为正式关系。
- 新增独立回忆题，明确显示答案后再手工标记；可查看真实来源页并返回原题，查看本身不评级。标记确认提交后才进入下一题，无虚构到期时间或算法熟练度。
- 增加含关系映射与降级说明的 Markdown / JSON Canvas 快照导出；完整学习资料仍用 `.iwbackup`。
- Schema 9 新增知识记录、不可变修订及操作回执三张表；整库备份覆盖 21 张作者表，读取 Schema 6/7/8 归档，并保留同签名覆盖安装路径。

实施范围与仍需完善项见 [UI-R2 实施记录](UI-R2-IMPLEMENTATION.md)，本轮真机项目单列于 [测试清单](DEVICE-TEST-CHECKLIST.md)。代码插件、OCR、AI 自动摘要、PDF、正式复习调度和云服务继续按启用条件后置。

## A3.5 · 学习工作台测试版 · 2026-09-26

版本：`0.0.10-a3.5-learning`，versionCode `10`，数据库 Schema `8`。交付源码为 `4b01998c65326a70fed30907de0071eb79173004`，本次整理文档不改变该 APK 的源码版本。

### 新增与完善

- **框选与套索**：选择完整笔迹，移动、原位复制后拖开、改色、删除；“只擦框内部分”精确擦除选区覆盖部分，支持撤销与重做。
- **稳线美化**：对选中的普通笔迹进行几何平滑，可调强度、对比原迹、预览、取消、确认及撤销。保留端点、锐角、时间与轴数据；暂不处理已局部擦除的笔迹及荧光笔。
- **摘录摘要卡**：从选区保留当时可见的原迹快照，手工填写标题和摘要，记录来源页、修订及区域，可返回来源。来源变化或回收时提示，原快照仍保留。
- **共享卡片、大纲与基础思维导图**：两种视图共用节点及卡片数据；支持子主题、调整上级、防止循环、同卡复用、拖动、平移缩放、适配全部和自动排布。移除叶节点保留卡片；有活动引用的卡片不能直接回收。
- **搜索与导出**：按摘要标题、正文搜索；Markdown 导出保留大纲层级和共享卡片链接，每张卡片正文只输出一次。
- **整库备份**：Schema 8 新增 5 张学习表；`.iwbackup` 覆盖 18 张作者数据表，包括卡片、版本、来源、节点及回执，并可读取 Schema 6/7 旧备份。
- **覆盖安装**：本地工作台包固定使用已有开发签名；新增 A3 versionCode 5 / Schema 4 到 A3.5 versionCode 10 / Schema 8 的实际保留数据升级验证。

### 修复

- 思维导图绘制越界覆盖学习工具栏：在原生视图绘制层裁剪。像素回归在旧包复现失败，在修复包通过，并检查实际截图。
- 美化预览取消后重开仍无法应用：使用独立来源快照及可重放状态，明确计算、就绪、失败和重试；5 秒超时，阻止旧强度及迟到结果覆盖当前请求。
- 页面移动／复制预览长时间停在“核对中”：读取结果改为可重放状态，8 秒超时覆盖 IO 派发和读取全程，支持重试。
- 美化弹窗内容可滚动，操作区独立；修复重复运行时资料库改名测试被同名旧数据干扰的问题。

### 已执行验证

| 项目 | 结果与范围 |
| --- | --- |
| 最终 CI | [run 36231757947](https://github.com/InkWeftDevs/InkWeft/actions/runs/36231757947)，提交 `4b01998`，成功 |
| JVM / Room / App | 181 + 109 + 46 = **336 项不同用例**，0 失败、0 错误、0 跳过 |
| CI 重复采图 | App 46 项再次通过，不重复计入 336 项 |
| CI 覆盖安装 | 同签名 A3.4 versionCode 9 → 10，两阶段验证通过 |
| 本地 Android 12 | Room 109 项通过；最终 APK 的选区学习与页面编辑定向 14 项通过 |
| 本地覆盖安装 | 同签名 A3 versionCode 5 → 10，`adb install -r`；种入数据后未卸载或清库，文字、原笔迹字节、收藏、当前页、索引及回执保留 |
| 构建与静态检查 | 构建成功；lint 0 错误 / 51 警告；Room Schema 无漂移；签名与 16 KiB zipalign 检查通过 |

额外的 MuMu 384dpi／自动旋转配置有 3 项失败（缩略图发现、封面选择、工具栏可见性），继续列入兼容性待办，不算入通过结果。预览状态修复已通过最终 CI 主测试和重复测试，但没有据此宣称所有设备问题已解决或原 Compose 内部根因已完全证明。

### 交付与边界

- 安装包：本地工作区 `dist/A3.5/InkWeft-A3.5-workspace-preview.apk`，37,958,841 字节。
- 应用名：**墨织工作台预览**；包名：`org.inkweft.app.a0.workspace`；Android 12 及以上（minSdk 31），targetSdk 36。
- APK SHA-256：`b510e3f119407f8033c14476c71250ac2ae2c685fb1ea6770d966f81bf435c23`。
- 签名证书 SHA-256：`18e67dbce88152dbe4d4821b5a3a513a409a3df4eac9261f4c11126c36e43969`。
- 构建回执、原始验证材料和最终截图见同目录 `build-receipt.json`、`verification.zip`、`screenshots-final/`。
- 相同包名、相同签名的升级路径已验证；应用显示名称相同不代表证书相同。历史 CI 临时签名可能不同，遇到冲突不能靠卸载清库处理。
- 摘要仍为手工内容；美化是几何稳线；未实现自动 OCR、AI 自动摘要或字体替换。普通 `.iwpage` / `.iwbook` 内容副本、整本复制暂不携带完整学习卡片，完整学习资料使用 `.iwbackup`。
- 真机笔感、掌拒、长时间温升和完整 37AX 尚未验收；撤销历史不跨进程重启。本版为测试包，PR #2 仍为 Draft，未合并或正式发行。

## A3—A3.4 · 历史功能概览

下表为按阶段归纳的功能记录，不将旧阶段的测试数量合并到本次统计。

| 阶段 | 已实现内容 | 详细记录 |
| --- | --- | --- |
| A3 | 局部橡皮、多页、封面与改名，原生笔迹保存 | [Android 历史说明](README.md) |
| A3.1 | 指定位置批量插页、可编辑笔盒预设 | Git 历史 `7a4e271`、`5717f86`、`1f6d35c` |
| A3.2 | 笔记复制、置顶、页面／整本内容导入导出 | [资料库说明](A3.2-LIBRARY.md) |
| A3.3 | 整库备份与恢复、数据身份及冲突校验 | [备份说明](A3.3-BACKUP.md) |
| A3.4 | 页面移动、复制、回收、恢复及指定目标位置 | [页面整理说明](A3.4-PAGES.md) |

## 文档与工作区整理 · 2026-09-26

- 增加本修改日志及按优先级排列的待实现清单。
- 更新 Android 入口和 A3.5 说明，纠正已过时的导出范围及预览验证状态。
- 工作区旧构建日志、诊断资料和一次性工具按阶段归档；安装包、最终证据、源码、构建依赖及签名密钥保留原位置。具体移动记录在工作区 `archives/2026-09-26/整理记录.md`，不作为应用功能变更。
